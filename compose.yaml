services:
  api:
    build: .
    ports:
      - "7372:7372"
    env_file:
      - path: .env
        required: false
    volumes:
      - ./app:/app/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 7372 --reload
